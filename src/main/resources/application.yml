spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/postgres
    username: postgres
    password: postgres
  jpa:
    show-sql: true

  liquibase:
    change-log: classpath:db/changelog/changelog-master.yml